export const metadata = {
  title: 'Pizzeria Valentino — Studio',
  robots: 'noindex, nofollow',
}

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return children
}
